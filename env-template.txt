# Copy file này thành .env và điền các API key thực tế
# Lưu ý: OpenAI API key giờ được bảo mật trong Vercel Serverless Function
# Không cần REACT_APP_API_KEY_OPENAI ở frontend nữa

# API Keys (Chỉ cần cho Gemini và Google TTS)
REACT_APP_GEMINI_API_KEY=your_gemini_api_key_here
REACT_APP_GOOGLE_TTS_KEY=your_google_tts_key_here

# App Configuration
REACT_APP_APP_NAME=TOEIC Vocabulary Practice
REACT_APP_VERSION=1.0.0
# Nếu frontend và API chạy khác domain (ví dụ: deploy FE lên Firebase, API trên Vercel)
# thì cấu hình base URL của API tại đây, ví dụ: https://toeic.thanhlaptrinh.online
# Để trống nếu dùng chung domain (vercel dev hoặc deploy đồng thời trên Vercel)
REACT_APP_API_BASE_URL=

# Lưu ý: Khi deploy lên Vercel:
# 1. Thêm OPENAI_API_KEY vào Environment Variables trên Vercel (không phải REACT_APP_)
# 2. OPENAI_API_KEY sẽ chỉ được sử dụng trong serverless function, không bao giờ xuất hiện ở frontend 